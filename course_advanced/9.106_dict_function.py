empty = dict()
print(empty)

with_values = dict(a1=1, b_=2, c_3=3)
print(with_values)

# ograniczenia w używaniu funkcji dict()
# -klucz musi zaczynac sie od litery, niedopuszczalne jest aby na początku
#  znajdowała się cyfra
# -klucz może składać się jedynie z liter, cyfr i znaku podłogi 